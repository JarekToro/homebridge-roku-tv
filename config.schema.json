{
  "pluginAlias": "RokuTV",
  "pluginType": "platform",
  "singular": true,
  "schema": {
    "type": "object",
    "properties": {
      "pollingInterval": {
        "title": "Polling Interval (ms)",
        "type": "integer",
        "description": "How often to query the Roku Tv for power state and active app"
      }
    }
  }
}
